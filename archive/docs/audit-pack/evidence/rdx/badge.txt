### Radix Themes @ 1faff10  packages/radix-ui-themes/src/components/badge.css
.rt-Badge {
  display: inline-flex;
  align-items: center;
  white-space: nowrap;
  font-family: var(--default-font-family);   /* = -apple-system, BlinkMacSystemFont, 'Segoe UI (Custom)', Roboto, 'Helvetica Neue', 'Open Sans (Custom)', system-ui,
    sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji' */
  font-weight: var(--font-weight-medium);   /* = 500 */
  font-style: normal;
  flex-shrink: 0;
  line-height: 1;

  /* Make sure that the height is not stretched in a Flex/Grid layout */
  height: fit-content;
}

/***************************************************************************************************
 *                                                                                                 *
 * SIZES                                                                                           *
 *                                                                                                 *
 ***************************************************************************************************/

@breakpoints {
  .rt-Badge {
    &:where(.rt-r-size-1) {
      font-size: var(--font-size-1);   /* = 12px */
      line-height: var(--line-height-1);   /* = 16px */
      letter-spacing: var(--letter-spacing-1);   /* = 0.0025em */
      padding: calc(var(--space-1) * 0.5) calc(var(--space-1) * 1.5);   /* = 2px 6px */
      gap: calc(var(--space-1) * 1.5);   /* = 6px */
      border-radius: max(var(--radius-1), var(--radius-full));   /* = 3px */
    }
    &:where(.rt-r-size-2) {
      font-size: var(--font-size-1);   /* = 12px */
      line-height: var(--line-height-1);   /* = 16px */
      letter-spacing: var(--letter-spacing-1);   /* = 0.0025em */
      padding: var(--space-1) var(--space-2);   /* = 4px 8px */
      gap: calc(var(--space-1) * 1.5);   /* = 6px */
      border-radius: max(var(--radius-2), var(--radius-full));   /* = 4px */
    }
    &:where(.rt-r-size-3) {
      font-size: var(--font-size-2);   /* = 14px */
      line-height: var(--line-height-2);   /* = 20px */
      letter-spacing: var(--letter-spacing-2);   /* = 0em */
      padding: var(--space-1) calc(var(--space-2) * 1.25);   /* = 4px 10px */
      gap: var(--space-2);   /* = 8px */
      border-radius: max(var(--radius-2), var(--radius-full));   /* = 4px */
    }
  }
}

/***************************************************************************************************
 *                                                                                                 *
 * VARIANTS                                                                                        *
 *                                                                                                 *
 ***************************************************************************************************/

/* solid */

.rt-Badge:where(.rt-variant-solid) {
  background-color: var(--accent-9);   /* = var(--amber-9) */
  color: var(--accent-contrast);   /* = var(--amber-contrast) */

  &::selection {
    background-color: var(--accent-7);   /* = var(--amber-7) */
    color: var(--accent-12);   /* = var(--amber-12) */
  }

  &:where(.rt-high-contrast) {
    background-color: var(--accent-12);   /* = var(--amber-12) */
    color: var(--accent-1);   /* = var(--amber-1) */

    &::selection {
      background-color: var(--accent-a11);   /* = var(--amber-a11) */
      color: var(--accent-1);   /* = var(--amber-1) */
    }
  }
}

/* surface */

.rt-Badge:where(.rt-variant-surface) {
  background-color: var(--accent-surface);   /* = var(--amber-surface) */
  box-shadow: inset 0 0 0 1px var(--accent-a6);   /* = inset 0 0 0 1px var(--amber-a6) */
  color: var(--accent-a11);   /* = var(--amber-a11) */

  &:where(.rt-high-contrast) {
    color: var(--accent-12);   /* = var(--amber-12) */
  }
}

/* soft */

.rt-Badge:where(.rt-variant-soft) {
  background-color: var(--accent-a3);   /* = var(--amber-a3) */
  color: var(--accent-a11);   /* = var(--amber-a11) */
  &:where(.rt-high-contrast) {
    color: var(--accent-12);   /* = var(--amber-12) */
  }
}

/* outline */

.rt-Badge:where(.rt-variant-outline) {
  box-shadow: inset 0 0 0 1px var(--accent-a8);   /* = inset 0 0 0 1px var(--amber-a8) */
  color: var(--accent-a11);   /* = var(--amber-a11) */
  &:where(.rt-high-contrast) {
    box-shadow:
      inset 0 0 0 1px var(--accent-a7),
      inset 0 0 0 1px var(--gray-a11);
    color: var(--accent-12);   /* = var(--amber-12) */
  }
}
