### Radix Themes @ 1faff10  packages/radix-ui-themes/src/components/radio-cards.css
@import './_internal/base-card.css';

.rt-RadioCardsRoot {
  line-height: var(--line-height);   /* = 20px */
  letter-spacing: var(--letter-spacing);   /* = 0em */
  cursor: default;
}

.rt-RadioCardsItem {
  --base-card-padding-top: var(--radio-cards-item-padding-y);   /* = 10px */
  --base-card-padding-right: var(--radio-cards-item-padding-x);   /* = 12px */
  --base-card-padding-bottom: var(--radio-cards-item-padding-y);   /* = 10px */
  --base-card-padding-left: var(--radio-cards-item-padding-x);   /* = 12px */
  --base-card-border-radius: var(--radio-cards-item-border-radius);   /* = 6px */
  --base-card-border-width: var(--radio-cards-item-border-width);   /* = 1px */

  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);   /* = 8px */

  & > * {
    /* Avoid unintentional drag interactions (e.g. on images) */
    pointer-events: none;
  }
  /* stylelint-disable-next-line selector-max-type */
  & > :where(svg) {
    /* Don’t squish icons */
    flex-shrink: 0;
  }
  &::after {
    outline-offset: -1px;
  }
}

/* * * * * * * * * * * * * * * * * * * */
/*                                     */
/*                Sizes                */
/*                                     */
/* * * * * * * * * * * * * * * * * * * */

@breakpoints {
  .rt-RadioCardsRoot {
    /* 40px height for the card with one line of text */
    &:where(.rt-r-size-1) {
      font-size: var(--font-size-2);   /* = 14px */
      --line-height: var(--line-height-2);   /* = 20px */
      --letter-spacing: var(--letter-spacing-2);   /* = 0em */
      --radio-cards-item-padding-x: var(--space-3);   /* = 12px */
      --radio-cards-item-padding-y: calc(var(--space-3) / 1.2);   /* = 10px */
      --radio-cards-item-border-radius: var(--radius-3);   /* = 6px */
    }
    /* 48px height for the card with one line of text */
    &:where(.rt-r-size-2) {
      font-size: var(--font-size-2);   /* = 14px */
      --line-height: var(--line-height-2);   /* = 20px */
      --letter-spacing: var(--letter-spacing-2);   /* = 0em */
      --radio-cards-item-padding-x: var(--space-4);   /* = 16px */
      --radio-cards-item-padding-y: calc(var(--space-4) * 0.875);   /* = 14px */
      --radio-cards-item-border-radius: var(--radius-3);   /* = 6px */
    }
    /* 64px height for the card with one line of text */
    &:where(.rt-r-size-3) {
      font-size: var(--font-size-3);   /* = 16px */
      --line-height: var(--line-height-3);   /* = 24px */
      --letter-spacing: var(--letter-spacing-3);   /* = 0em */
      --radio-cards-item-padding-x: var(--space-5);   /* = 24px */
      --radio-cards-item-padding-y: calc(var(--space-5) / 1.2);   /* = 20px */
      --radio-cards-item-border-radius: var(--radius-4);   /* = 8px */
    }
  }
}

/* * * * * * * * * * * * * * * * * * * */
/*                                     */
/*         Variants / Surface          */
/*                                     */
/* * * * * * * * * * * * * * * * * * * */

:where(.rt-RadioCardsRoot.rt-variant-surface) {
  .rt-RadioCardsItem {
    --radio-cards-item-border-width: 1px;
    --radio-cards-item-background-color: var(--color-surface);   /* = rgba(255, 255, 255, 0.85) */

    &::before {
      background-color: var(--radio-cards-item-background-color);   /* = rgba(255, 255, 255, 0.85) */
    }
    &::after {
      box-shadow: var(--base-card-surface-box-shadow);
    }
    @media (hover: hover) {
      &:where(:not(:disabled):not([data-state='checked']):hover) {
        &::after {
          box-shadow: var(--base-card-surface-hover-box-shadow);
        }
      }
    }
  }
}

/* * * * * * * * * * * * * * * * * * * */
/*                                     */
/*         Variants / Classic          */
/*                                     */
/* * * * * * * * * * * * * * * * * * * */

:where(.rt-RadioCardsRoot.rt-variant-classic) {
  .rt-RadioCardsItem {
    --radio-cards-item-border-width: 1px;
    --radio-cards-item-background-color: var(--color-surface);   /* = rgba(255, 255, 255, 0.85) */

    transition: box-shadow 120ms;
    box-shadow: var(--base-card-classic-box-shadow-outer);

    &::before {
      background-color: var(--radio-cards-item-background-color);   /* = rgba(255, 255, 255, 0.85) */
    }
    &::after {
      box-shadow: var(--base-card-classic-box-shadow-inner);
    }
    @media (hover: hover) {
      &:where(:not(:disabled):not([data-state='checked']):hover) {
        transition-duration: 40ms;
        box-shadow: var(--base-card-classic-hover-box-shadow-outer);
        &::after {
          box-shadow: var(--base-card-classic-hover-box-shadow-inner);
        }
      }
    }
  }
}

/* * * * * * * * * * * * * * * * * * * */
/*                                     */
/*             All checked             */
/*                                     */
/* * * * * * * * * * * * * * * * * * * */

.rt-RadioCardsItem:where([data-state='checked']) {
  &::after {
    outline: 2px solid var(--accent-indicator);   /* = 2px solid var(--amber-indicator) */
  }
  :where(.rt-RadioCardsRoot.rt-high-contrast) & {
    &::after {
      outline-color: var(--accent-12);   /* = var(--amber-12) */
    }
  }
}

/* * * * * * * * * * * * * * * * * * * */
/*                                     */
/*              All focus              */
/*                                     */
/* * * * * * * * * * * * * * * * * * * */

.rt-RadioCardsItem:where(:focus-visible) {
  &::after {
    outline: 2px solid var(--focus-8);   /* = 2px solid var(--amber-8) */
  }
  &:where([data-state='checked']) {
    &::before {
      background-image: linear-gradient(var(--focus-a3), var(--focus-a3));   /* = linear-gradient(var(--amber-a3), var(--amber-a3)) */
    }
    &::after {
      outline-color: var(--focus-10);   /* = var(--amber-10) */
    }
  }
}

/* * * * * * * * * * * * * * * * * * * */
/*                                     */
/*            All disabled             */
/*                                     */
/* * * * * * * * * * * * * * * * * * * */

.rt-RadioCardsItem:where(:disabled) {
  cursor: var(--cursor-disabled);   /* = not-allowed */
  color: var(--gray-a9);   /* = var(--mauve-a9) */

  &::selection {
    background-color: var(--gray-a5);   /* = var(--mauve-a5) */
  }
  &::before {
    background-image: linear-gradient(var(--gray-a2), var(--gray-a2));   /* = linear-gradient(var(--mauve-a2), var(--mauve-a2)) */
  }
  &::after {
    outline-color: var(--gray-8);   /* = var(--mauve-8) */
  }
}
