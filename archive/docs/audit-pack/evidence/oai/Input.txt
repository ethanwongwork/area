### OpenAI Apps SDK UI @ 0f00143  src/components/Input/Input.module.css
.Container {
  display: flex;
  align-items: center;
  gap: var(--input-gap);   /* = 4px */
  width: 100%;
  border-radius: var(--input-radius);   /* = 6px */
  color: var(--input-text-color);   /* = light-dark(#0d0d0d, #ffffff) */
  cursor: text;
  /* NOTE: Whenever this is not 16px on mobile, zooming occurs. */
  font-size: var(--input-font-size);   /* = 12px */
  font-weight: var(--font-weight-normal);   /* = 400 */
  transition-duration: var(--transition-duration-basic);   /* = 150ms */
  transition-property: color, box-shadow, background-color;
  transition-timing-function: var(--transition-ease-basic);   /* = ease */

  &:where([data-optically-align="start"]) {
    margin-inline-start: calc(var(--input-gutter) * -1);   /* = -8px */
  }

  &:where([data-optically-align="end"]) {
    margin-inline-end: calc(var(--input-gutter) * -1);   /* = -8px */
  }

  /* =============================================
    Outline variant
  ============================================= */
  &:where([data-variant="outline"]) {
    /* NOTE: Don't add box-model size here, as the <input> defines all height sizing */
    box-shadow: 0 0 0 1px var(--input-outline-border-color) inset;   /* = 0 0 0 1px light-dark(alpha(light-dark(#0d0d0d, #ffffff), 16%), alpha(light-dark(#0d0d0d, #ffffff), 25%)) inset */

    &:not([data-disabled]) {
      @mixin hover {
        box-shadow: 0 0 0 1px var(--input-outline-border-color-hover) inset;   /* = 0 0 0 1px light-dark(alpha(light-dark(#0d0d0d, #ffffff), 25%), alpha(light-dark(#0d0d0d, #ffffff), 30%)) inset */
      }

      &[data-focused="true"] {
        box-shadow: 0 0 0 1px var(--input-outline-border-color-focus) inset;   /* = 0 0 0 1px alpha(light-dark(#0d0d0d, #ffffff), 50%) inset */
      }

      &[data-invalid] {
        box-shadow: 0 0 0 1px var(--input-border-color-invalid) inset;   /* = 0 0 0 1px light-dark(#e02e2a, #ba2623) inset */
      }
    }
  }

  /* =============================================
    Soft variant
  ============================================= */
  &:where([data-variant="soft"]) {
    background-color: var(--input-soft-background-color);   /* = light-dark(alpha(light-dark(#0d0d0d, #ffffff), 8%), alpha(light-dark(#0d0d0d, #ffffff), 12%)) */

    &:not([data-disabled]) {
      &[data-focused="true"] {
        box-shadow: 0 0 0 1px var(--input-soft-border-color-focus) inset;   /* = 0 0 0 1px alpha(light-dark(#0d0d0d, #ffffff), 20%) inset */
      }

      &[data-invalid] {
        box-shadow: 0 0 0 1px var(--input-border-color-invalid) inset;   /* = 0 0 0 1px light-dark(#e02e2a, #ba2623) inset */
      }
    }
  }

  /* =============================================
    Invalid
  ============================================= */
  &[data-invalid] {
    ::selection {
      background-color: var(--red-a50);   /* = alpha(#fa423e, 16%) */
    }
  }

  /* =============================================
    Sizes
  ============================================= */
  &:where([data-size="3xs"]) {
    --input-size: var(--control-size-3xs);   /* = 22px */
    --input-font-size: var(--control-font-size-sm);   /* = 12px */
    --input-radius: var(--control-radius-sm);   /* = 6px */
    --input-gutter: var(--control-gutter-xs);   /* = 8px */
    --input-gap: var(--input-gap-xs);   /* = 4px */
    --input-adornment-offset: -1px;
  }

  &:where([data-size="2xs"]) {
    --input-size: var(--control-size-2xs);   /* = 24px */
    --input-font-size: var(--control-font-size-sm);   /* = 12px */
    --input-radius: var(--control-radius-sm);   /* = 6px */
    --input-gutter: var(--control-gutter-xs);   /* = 8px */
    --input-gap: var(--input-gap-xs);   /* = 4px */
    --input-adornment-offset: -1px;
  }

  &:where([data-size="xs"]) {
    --input-size: var(--control-size-xs);   /* = 26px */
    --input-font-size: var(--control-font-size-md);   /* = 14px */
    --input-radius: var(--control-radius-sm);   /* = 6px */
    --input-gutter: var(--control-gutter-xs);   /* = 8px */
    --input-gap: var(--input-gap-sm);   /* = 6px */
    --input-adornment-offset: -1px;
  }

  &:where([data-size="sm"]) {
    --input-size: var(--control-size-sm);   /* = 28px */
    --input-font-size: var(--control-font-size-md);   /* = 14px */
    --input-radius: var(--control-radius-md);   /* = 8px */
    --input-gutter: var(--control-gutter-sm);   /* = 10px */
    --input-gap: var(--input-gap-md);   /* = 8px */
    --input-adornment-offset: -1px;
  }

  &:where([data-size="md"]) {
    --input-size: var(--control-size-md);   /* = 32px */
    --input-font-size: var(--control-font-size-md);   /* = 14px */
    --input-radius: var(--control-radius-md);   /* = 8px */
    --input-gutter: var(--control-gutter-md);   /* = 12px */
    --input-gap: var(--input-gap-md);   /* = 8px */
  }

  &:where([data-size="lg"]) {
    --input-size: var(--control-size-lg);   /* = 36px */
    --input-font-size: var(--control-font-size-md);   /* = 14px */
    --input-radius: var(--control-radius-md);   /* = 8px */
    --input-gutter: var(--control-gutter-md);   /* = 12px */
    --input-gap: var(--input-gap-md);   /* = 8px */
  }

  &:where([data-size="xl"]) {
    --input-size: var(--control-size-xl);   /* = 40px */
    --input-font-size: var(--control-font-size-lg);   /* = 16px */
    --input-radius: var(--control-radius-lg);   /* = 10px */
    --input-gutter: var(--control-gutter-lg);   /* = 14px */
    --input-gap: var(--input-gap-lg);   /* = 10px */
  }

  &:where([data-size="2xl"]) {
    --input-size: var(--control-size-2xl);   /* = 44px */
    --input-font-size: var(--control-font-size-lg);   /* = 16px */
    --input-radius: var(--control-radius-xl);   /* = 12px */
    --input-gutter: var(--control-gutter-xl);   /* = 16px */
    --input-gap: var(--input-gap-lg);   /* = 10px */
    --input-adornment-offset: -4px;
  }

  &:where([data-size="3xl"]) {
    --input-size: var(--control-size-3xl);   /* = 48px */
    --input-font-size: var(--control-font-size-lg);   /* = 16px */
    --input-radius: var(--control-radius-xl);   /* = 12px */
    --input-gutter: var(--control-gutter-xl);   /* = 16px */
    --input-gap: var(--input-gap-lg);   /* = 10px */
    --input-adornment-offset: -4px;
  }

  /* =============================================
    Gutter sizes
  ============================================= */
  &:where([data-gutter-size="2xs"]) {
    --input-gutter: var(--control-gutter-2xs);   /* = 6px */
    --input-adornment-offset: -1px;
  }

  &:where([data-gutter-size="xs"]) {
    --input-gutter: var(--control-gutter-xs);   /* = 8px */
    --input-adornment-offset: -1px;
  }

  &:where([data-gutter-size="sm"]) {
    --input-gutter: var(--control-gutter-sm);   /* = 10px */
    --input-adornment-offset: -2px;
  }

  &:where([data-gutter-size="md"]) {
    --input-gutter: var(--control-gutter-md);   /* = 12px */
    --input-adornment-offset: -2px;
  }

  &:where([data-gutter-size="lg"]) {
    --input-gutter: var(--control-gutter-lg);   /* = 14px */
    --input-adornment-offset: -2px;
  }

  &:where([data-gutter-size="xl"]) {
    --input-gutter: var(--control-gutter-xl);   /* = 16px */
    --input-adornment-offset: -4px;
  }

  /* =============================================
    Pill
  ============================================= */
  &:where([data-pill]) {
    --input-radius: var(--radius-full);   /* = 9999px */
  }

  /* =============================================
    Disabled
  ============================================= */
  &[data-disabled] {
    cursor: not-allowed;
    opacity: 0.5;
  }

  /* =============================================
    Gutter
  ============================================= */
  /*
   * When adornments are present, gutter is applied to the container instead of the input.
   */
  &:where([data-has-start-adornment]) {
    padding-left: var(--input-gutter);   /* = 8px */
  }

  &:where([data-has-start-adornment][data-pill]) {
    padding-left: calc(var(--input-gutter) * var(--control-gutter-pill-scaling));   /* = 10.64px */
  }

  &:where([data-has-end-adornment]) {
    padding-right: var(--input-gutter);   /* = 8px */
  }

  &:where([data-has-end-adornment][data-pill]) {
    padding-right: calc(var(--input-gutter) * var(--control-gutter-pill-scaling));   /* = 10.64px */
  }

  /* =============================================
    Adornments
  ============================================= */
  > :first-child:not(input) {
    margin-left: var(--input-adornment-offset, -2px);   /* = -1px */
  }

  > :last-child:not(input) {
    margin-right: var(--input-adornment-offset, -2px);   /* = -1px */
  }
}

.Input {
  display: block;
  width: 100%;
  height: var(--input-size);   /* = 22px */
  /* `padding` is intrinsic to `.Input` when no adornments are present, for a few reasons. Autofill extensions are one such reason. */
  padding: 0 var(--input-gutter);   /* = 0 8px */
  border: 0;
  border-radius: 0;
  background: none;
  box-shadow: none;
  color: inherit;
  font-size: inherit;
  font-weight: inherit;
  line-height: inherit;

  &:focus {
    outline: 0;
  }

  &:disabled {
    cursor: not-allowed;
  }

  &::placeholder {
    color: var(--input-placeholder-text-color);   /* = light-dark(light-dark(#8f8f8f, #414141), light-dark(#414141, #8f8f8f)) */
  }

  /* Applying an animation on autofill allows us to listen for the event in JS */
  &:-webkit-autofill {
    animation: native-autofill-in 1ms;
  }

  @mixin autofill {
    color: var(--color-text) !important;   /* = light-dark(#0d0d0d, #ffffff) !important */
    -webkit-text-fill-color: var(--color-text) !important;   /* = light-dark(#0d0d0d, #ffffff) !important */
    /*
     * Prevent all color changes by delaying their effect for a very long time
     * We cannot beat the specificity of user-agent stylesheet declarations, so we must delay them.
     */
    transition:
      background-color 0s 50000s,
      box-shadow 0s 50000s,
      filter 0s 50000s !important;
  }

  /* Prevent strange autofill size adjustments */
  &:-webkit-autofill::first-line {
    font-size: inherit !important;
    transition: font-size 0s 50000s !important;
  }

  /* Hide up/down arrows */
  &[type="number"] {
    /* Firefox */
    appearance: textfield;

    /* Chrome, Safari, Edge, Opera */
    &::-webkit-outer-spin-button,
    &::-webkit-inner-spin-button {
      margin: 0;
      appearance: none;
    }
  }

  /* Dynamic pill spacing */
  :where(.Container[data-pill]) & {
    padding: 0 calc(var(--input-gutter) * var(--control-gutter-pill-scaling));   /* = 0 10.64px */
  }

  :where(.Container[data-has-start-adornment]) & {
    padding-left: 0;
  }

  :where(.Container[data-has-end-adornment]) & {
    padding-right: 0;
  }
}

@keyframes native-autofill-in {
  0% {
    opacity: 1;
  }

  100% {
    opacity: 1;
  }
}
