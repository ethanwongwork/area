### OpenAI Apps SDK UI @ 0f00143  src/components/Menu/Menu.module.css
.Menu {
  /* Top-level selector needs to exist */
}

.MenuList {
  overflow: auto;
  width: var(--menu-width, auto);   /* = auto */
  min-width: var(--menu-min-width, 300px);   /* = 300px */
  max-width: var(--radix-dropdown-menu-content-available-width);
  max-height: min(
    var(--radix-dropdown-menu-content-available-height),
    var(--menu-max-height, 1200px)
  );
  padding: var(--menu-gutter);   /* = 6px */
  border-radius: var(--menu-radius);   /* = 12px */
  background: var(--color-surface-elevated);   /* = light-dark(light-dark(#ffffff, #0d0d0d), light-dark(#afafaf, #303030)) */
  box-shadow: var(--shadow), var(--shadow-hairline);   /* = 0 10px 15px -3px light-dark(rgba(0 0 0 / 10%), rgba(0 0 0 / 20%)),
      0 4px 6px -4px light-dark(rgba(0 0 0 / 10%), rgba(0 0 0 / 20%)), 0 0 rgb(0 0 0 / 0%) */
  color: var(--color-text);   /* = light-dark(#0d0d0d, #ffffff) */
  font-size: var(--menu-font-size);   /* = 14px */
  line-height: var(--menu-line-height);   /* = 20px */
  -webkit-overflow-scrolling: touch;
  pointer-events: auto;
  /* The transform-origin computed from the content and arrow positions/offsets */
  transform-origin: var(--radix-dropdown-menu-content-transform-origin);
  will-change: transform;

  /* Animation states applied to a wrapping parent */
  .Menu[data-entering] & {
    opacity: 0;
    transform: scale(0.95);
  }

  .Menu[data-entering-active] &,
  .Menu[data-entering][data-interrupted] & {
    opacity: 1;
    transform: scale(1);
    transition-duration: 0.35s;
    transition-property: opacity, transform;
    transition-timing-function: var(--cubic-enter);   /* = cubic-bezier(0.19, 1, 0.22, 1) */
  }

  .Menu[data-exiting] & {
    opacity: 1;
    transform: scale(1);
  }

  .Menu[data-exiting-active] &,
  .Menu[data-exiting][data-interrupted] & {
    opacity: 0;
    transform: scale(0.95);
    transition-duration: 0.2s;
    transition-property: opacity, transform;
    transition-timing-function: var(--cubic-exit);   /* = cubic-bezier(0.8, 0, 0.4, 1) */
  }
}

.MenuItemContent {
  padding: var(--menu-item-padding);   /* = 6px 8px */
}

.MenuItem {
  position: relative;
  display: block;
  /* Defaults to inherit from pressable wrapper, this container is not flex */
  align-items: center;
  gap: var(--menu-item-gap);   /* = 6px */
  padding: var(--menu-item-padding);   /* = 6px 8px */
  color: var(--color-text);   /* = light-dark(#0d0d0d, #ffffff) */
  cursor: pointer;
  user-select: none;

  &:focus {
    outline: none;
  }

  @mixin hover {
    /* Prevent links from changing colors */
    color: var(--color-text);   /* = light-dark(#0d0d0d, #ffffff) */
  }

  &::before {
    position: absolute;
    inset: 0;
    display: block;
    border-radius: calc(var(--menu-radius) - var(--menu-gutter));   /* = 6px */
    background: var(--menu-item-background-color);   /* = light-dark(alpha(light-dark(#0d0d0d, #ffffff), 8%), alpha(light-dark(#0d0d0d, #ffffff), 10%)) */
    content: "";
    opacity: 0;
    transform: scale(var(--scale, 0.98));   /* = scale(0.98) */
    transition-duration: var(--transition-duration-basic);   /* = 150ms */
    transition-property: opacity, background-color, transform;
    transition-timing-function: var(--cubic-enter);   /* = cubic-bezier(0.19, 1, 0.22, 1) */
    will-change: opacity, transform;
  }

  &[data-state="open"],
  &[data-highlighted] {
    &::before {
      opacity: 1;
      transform: scale(1);
    }

    &:has(.ItemActions:hover)::before {
      opacity: 0.4;
    }

    &:not(:has(.ItemActions:hover)):active::before {
      transform: scale(var(--scale, 0.98));   /* = scale(0.98) */
    }

    .ItemActions {
      opacity: 1;
    }
  }

  &[data-disabled] {
    cursor: not-allowed;
    opacity: 0.5;

    &::before {
      display: none;
    }
  }
}

.PressableInner {
  position: relative;
  display: flex;
  /* Allow flex settings to be applied to the container, for className ergonomics */
  align-items: inherit;
  justify-content: inherit;
  gap: inherit;
}

.ItemActions {
  /* Prevent adding height to the MenuItem, and horizontally center */
  display: flex;
  align-items: center;
  height: 0;
  /* Magic number that pulls back the icon an equal gutter from the top/bottom */
  margin-right: -5px;
  opacity: 0;
  transition-duration: var(--transition-duration-basic);   /* = 150ms */
  transition-property: opacity;
  transition-timing-function: var(--cubic-enter);   /* = cubic-bezier(0.19, 1, 0.22, 1) */
}

.Separator {
  height: 1px;
  margin: var(--menu-separator-gutter);   /* = 6px -6px */
  background: var(--menu-separator-background-color);   /* = light-dark(alpha(light-dark(#0d0d0d, #ffffff), 10%), alpha(light-dark(#0d0d0d, #ffffff), 12%)) */
}

.SubMenuItem {
  justify-content: space-between;
}

.SubTriggerIcon {
  /* slight visual offset of svg canvas */
  margin-right: -2px;
  opacity: 0.75;
  transition: opacity var(--transition-duration-basic) var(--transition-ease-basic);   /* = opacity 150ms ease */

  .SubMenuItem[data-state="open"] &,
  .SubMenuItem[data-highlighted] & {
    opacity: 1;
  }
}

.RadioItem {
  gap: spacing(2);   /* = 8px */

  :where([data-indicator-position="end"]) & {
    justify-content: space-between;
    gap: spacing(3);   /* = 12px */
  }
}

.RadioIndicatorActive {
  position: relative;
  display: inline-grid;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;

  &::before,
  &::after {
    display: block;
    content: "";
    grid-column-start: 1;
    grid-row-start: 1;
    place-self: center center;
    will-change: transform;
  }

  /* Filled circle */
  &::before {
    width: var(--menu-radio-indicator-size);   /* = 18px */
    height: var(--menu-radio-indicator-size);   /* = 18px */
    border-radius: var(--radius-full);   /* = 9999px */
    animation: fade-in 0.4s var(--cubic-enter);   /* = fade-in 0.4s cubic-bezier(0.19, 1, 0.22, 1) */
    background-color: var(--radio-group-indicator-background-color);   /* = light-dark(light-dark(#181818, #ededed), light-dark(#131313, #f3f3f3)) */
  }

  /*  Empty middle circle */
  &::after {
    width: var(--menu-radio-indicator-hole-size);   /* = 8px */
    height: var(--menu-radio-indicator-hole-size);   /* = 8px */
    border-radius: var(--radius-full);   /* = 9999px */
    animation: scale-in 0.4s var(--cubic-enter);   /* = scale-in 0.4s cubic-bezier(0.19, 1, 0.22, 1) */
    background-color: var(--radio-group-indicator-hole-background-color);   /* = light-dark(#ffffff, #0d0d0d) */
  }

  /* Don't play the animation when entering */
  [data-entering] &::before,
  [data-entering] &::after {
    animation-duration: 0s;
  }
}

.RadioIndicator {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  width: var(--menu-radio-indicator-size);   /* = 18px */
  height: var(--menu-radio-indicator-size);   /* = 18px */
  padding: 0;
  border: none;
  border-radius: var(--radius-full);   /* = 9999px */
  background-color: transparent;
  box-shadow: 0 0 0 1px var(--radio-group-indicator-border-color) inset;   /* = 0 0 0 1px light-dark(alpha(light-dark(#0d0d0d, #ffffff), 16%), alpha(light-dark(#0d0d0d, #ffffff), 25%)) inset */
  cursor: pointer;
  transition-duration: var(--transition-duration-basic);   /* = 150ms */
  transition-property: box-shadow;
  transition-timing-function: var(--transition-ease-basic);   /* = ease */

  [data-indicator-position="end"] & {
    order: 1;
  }

  [data-highlighted] & {
    box-shadow: 0 0 0 1px var(--radio-group-indicator-border-color-hover) inset;   /* = 0 0 0 1px alpha(light-dark(#0d0d0d, #ffffff), 25%) inset */
  }

  [data-disabled] & {
    cursor: not-allowed;
  }

  /* Can't be focused here anyway */
  &:focus {
    outline: none;
  }
}

@keyframes scale-in {
  0% {
    transform: scale(0);
  }

  100% {
    transform: scale(1);
  }
}

@keyframes fade-in {
  0% {
    opacity: 0;
  }

  100% {
    opacity: 1;
  }
}

.CheckboxItem {
  gap: spacing(2);   /* = 8px */

  &:where([data-indicator-position="end"]) {
    justify-content: space-between;
    gap: spacing(3);   /* = 12px */
  }
}

.CheckboxIndicator {
  flex-shrink: 0;
  width: var(--menu-checkbox-indicator-size);   /* = 18px */
  height: var(--menu-checkbox-indicator-size);   /* = 18px */

  [data-indicator-position="end"] & {
    order: 1;
  }

  [data-indicator-variant="solid"] & {
    border-radius: var(--radius-full);   /* = 9999px */
    box-shadow: 0 0 0 1px var(--radio-group-indicator-border-color) inset;   /* = 0 0 0 1px light-dark(alpha(light-dark(#0d0d0d, #ffffff), 16%), alpha(light-dark(#0d0d0d, #ffffff), 25%)) inset */
  }
}

.CheckboxCircle {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  width: var(--menu-checkbox-indicator-size);   /* = 18px */
  height: var(--menu-checkbox-indicator-size);   /* = 18px */
  border-radius: var(--radius-full);   /* = 9999px */
  animation: fade-in 0.5s var(--cubic-enter);   /* = fade-in 0.5s cubic-bezier(0.19, 1, 0.22, 1) */
  background-color: var(--radio-group-indicator-background-color);   /* = light-dark(light-dark(#181818, #ededed), light-dark(#131313, #f3f3f3)) */
  color: var(--radio-group-indicator-hole-background-color);   /* = light-dark(#ffffff, #0d0d0d) */

  /* Don't play the animation when entering */
  [data-entering] &::before,
  [data-entering] &::after {
    animation-duration: 0s;
  }
}
