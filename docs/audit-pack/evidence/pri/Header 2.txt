### Primer React @ e4e6eba  packages/react/src/Header/Header.module.css
.Header {
  z-index: 32;
  display: flex;
  padding: var(--base-size-16);   /* = 16px */
  overflow: auto;
  font-size: var(--text-body-size-medium);   /* = 14px */
  line-height: var(--text-title-lineHeight-large);   /* = 1.5 */
  color: var(--header-fgColor-default);
  background-color: var(--header-bgColor);
  align-items: center;
  flex-wrap: nowrap;
}

.HeaderItem {
  display: flex;
  margin-right: var(--base-size-16);   /* = 16px */
  align-self: stretch;
  align-items: center;
  flex-wrap: nowrap;

  &:where([data-full]) {
    flex: auto;
  }
}

.HeaderLink {
  display: flex;
  font-weight: var(--text-title-weight-large);   /* = 600 */
  color: var(--header-fgColor-logo);
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  align-items: center;

  &:hover,
  &:focus {
    color: var(--header-fgColor-default);
  }
}
### Primer React @ e4e6eba  packages/react/src/deprecated/ActionList/Header.module.css
.Header {
  /* 6px vertical padding + 20px line height = 32px total height
   *
   * TODO: When rem-based spacing on a 4px scale lands, replace
   * hardcoded '6px' with 'calc((${get('space.s32')} - ${get('space.20')}) / 2)'.
   */
  /* stylelint-disable-next-line primer/spacing */
  padding: 6px var(--base-size-16);   /* = 6px 16px */
  font-size: var(--text-body-size-small);   /* = 12px */
  font-weight: var(--text-title-weight-large);   /* = 600 */
  color: var(--fgColor-muted);

  &:where([data-filled]) {
    background: var(--bgColor-muted);
    margin: var(--base-size-8) 0;   /* = 8px 0 */
    /* stylelint-disable-next-line primer/colors */
    border-top: var(--borderWidth-thin) solid var(--bgColor-neutral-muted);   /* = 1px solid var(--bgColor-neutral-muted) */
    /* stylelint-disable-next-line primer/colors */
    border-bottom: var(--borderWidth-thin) solid var(--bgColor-neutral-muted);   /* = 1px solid var(--bgColor-neutral-muted) */

    &:first-child {
      margin-top: 0;
    }
  }
}
