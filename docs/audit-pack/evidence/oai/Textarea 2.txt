### OpenAI Apps SDK UI @ 0f00143  src/components/Textarea/Textarea.module.css
.Container {
  --textarea-line-height: 20px;
  --textarea-font-weight: var(--font-weight-normal);   /* = 400 */
  --textarea-color: var(--color-text);   /* = light-dark(#0d0d0d, #ffffff) */
  --textarea-gutter-y: calc(0.5 * calc(var(--textarea-base-height) - var(--textarea-line-height)));   /* = 1px */
  --textarea-min-height: calc(
    (var(--textarea-gutter-y) * 2) + (var(--textarea-line-height) * var(--textarea-min-rows))
  );
  --textarea-max-height: calc(
    (var(--textarea-gutter-y) * 2) + (var(--textarea-line-height) * var(--textarea-max-rows))
  );

  /* ============================================ */
  /* Sizes */
  /* ============================================ */
  &:where([data-size="3xs"]) {
    --textarea-base-height: var(--control-size-3xs);   /* = 22px */
    --textarea-font-size: var(--control-font-size-sm);   /* = 12px */
    --textarea-radius: var(--control-radius-sm);   /* = 6px */
    --textarea-gutter-x: var(--control-gutter-xs);   /* = 8px */
  }

  &:where([data-size="2xs"]) {
    --textarea-base-height: var(--control-size-2xs);   /* = 24px */
    --textarea-font-size: var(--control-font-size-sm);   /* = 12px */
    --textarea-radius: var(--control-radius-sm);   /* = 6px */
    --textarea-gutter-x: var(--control-gutter-xs);   /* = 8px */
  }

  &:where([data-size="xs"]) {
    --textarea-base-height: var(--control-size-xs);   /* = 26px */
    --textarea-font-size: var(--control-font-size-md);   /* = 14px */
    --textarea-radius: var(--control-radius-sm);   /* = 6px */
    --textarea-gutter-x: var(--control-gutter-xs);   /* = 8px */
  }

  &:where([data-size="sm"]) {
    --textarea-base-height: var(--control-size-sm);   /* = 28px */
    --textarea-font-size: var(--control-font-size-md);   /* = 14px */
    --textarea-radius: var(--control-radius-md);   /* = 8px */
    --textarea-gutter-x: var(--control-gutter-sm);   /* = 10px */
  }

  /* ("md" is applied by default, set above) */
  &:where([data-size="md"]) {
    --textarea-base-height: var(--control-size-md);   /* = 32px */
    --textarea-font-size: var(--control-font-size-md);   /* = 14px */
    --textarea-radius: var(--control-radius-md);   /* = 8px */
    --textarea-gutter-x: var(--control-gutter-md);   /* = 12px */
  }

  &:where([data-size="lg"]) {
    --textarea-base-height: var(--control-size-lg);   /* = 36px */
    --textarea-font-size: var(--control-font-size-md);   /* = 14px */
    --textarea-radius: var(--control-radius-md);   /* = 8px */
    --textarea-gutter-x: var(--control-gutter-md);   /* = 12px */
  }

  &:where([data-size="xl"]) {
    --textarea-base-height: var(--control-size-xl);   /* = 40px */
    --textarea-font-size: var(--control-font-size-lg);   /* = 16px */
    --textarea-radius: var(--control-radius-lg);   /* = 10px */
    --textarea-gutter-x: var(--control-gutter-lg);   /* = 14px */
  }

  &:where([data-size="2xl"]) {
    --textarea-base-height: var(--control-size-2xl);   /* = 44px */
    --textarea-font-size: var(--control-font-size-lg);   /* = 16px */
    --textarea-radius: var(--control-radius-xl);   /* = 12px */
    --textarea-gutter-x: var(--control-gutter-xl);   /* = 16px */
  }

  &:where([data-size="3xl"]) {
    --textarea-base-height: var(--control-size-3xl);   /* = 48px */
    --textarea-font-size: var(--control-font-size-lg);   /* = 16px */
    --textarea-radius: var(--control-radius-xl);   /* = 12px */
    --textarea-gutter-x: var(--control-gutter-xl);   /* = 16px */
  }

  /* ============================================ */
  /* Gutter sizes */
  /* ============================================ */
  &:where([data-gutter-size="2xs"]) {
    --textarea-gutter-x: var(--control-gutter-2xs);   /* = 6px */
  }

  &:where([data-gutter-size="xs"]) {
    --textarea-gutter-x: var(--control-gutter-xs);   /* = 8px */
  }

  &:where([data-gutter-size="sm"]) {
    --textarea-gutter-x: var(--control-gutter-sm);   /* = 10px */
  }

  &:where([data-gutter-size="md"]) {
    --textarea-gutter-x: var(--control-gutter-md);   /* = 12px */
  }

  &:where([data-gutter-size="lg"]) {
    --textarea-gutter-x: var(--control-gutter-lg);   /* = 14px */
  }

  &:where([data-gutter-size="xl"]) {
    --textarea-gutter-x: var(--control-gutter-xl);   /* = 16px */
  }

  position: relative;
  overflow: hidden;
  display: flex;
  width: 100%;
  border-radius: var(--textarea-radius);   /* = 6px */
  color: var(--textarea-color);   /* = light-dark(#0d0d0d, #ffffff) */
  cursor: text;
  font-size: var(--textarea-font-size);   /* = 12px */
  font-weight: var(--textarea-font-weight);   /* = 400 */
  transition-duration: var(--transition-duration-basic);   /* = 150ms */
  transition-property: color, box-shadow, background-color;
  transition-timing-function: var(--transition-ease-basic);   /* = ease */

  /* =============================================
    Outline variant
  ============================================= */
  &:where([data-variant="outline"]) {
    /* NOTE: Don't add box-model size here, as the <textarea> defines all height sizing */
    box-shadow: 0 0 0 1px var(--input-outline-border-color) inset;   /* = 0 0 0 1px light-dark(alpha(light-dark(#0d0d0d, #ffffff), 16%), alpha(light-dark(#0d0d0d, #ffffff), 25%)) inset */

    &:not([data-disabled]) {
      @mixin hover {
        box-shadow: 0 0 0 1px var(--input-outline-border-color-hover) inset;   /* = 0 0 0 1px light-dark(alpha(light-dark(#0d0d0d, #ffffff), 25%), alpha(light-dark(#0d0d0d, #ffffff), 30%)) inset */
      }

      &[data-focused="true"] {
        box-shadow: 0 0 0 1px var(--input-outline-border-color-focus) inset;   /* = 0 0 0 1px alpha(light-dark(#0d0d0d, #ffffff), 50%) inset */
      }

      &[data-invalid] {
        box-shadow: 0 0 0 1px var(--input-border-color-invalid) inset;   /* = 0 0 0 1px light-dark(#e02e2a, #ba2623) inset */
      }
    }
  }

  /* =============================================
    Soft variant
  ============================================= */
  &:where([data-variant="soft"]) {
    background-color: var(--input-soft-background-color);   /* = light-dark(alpha(light-dark(#0d0d0d, #ffffff), 8%), alpha(light-dark(#0d0d0d, #ffffff), 12%)) */

    &:not([data-disabled]) {
      &[data-focused="true"] {
        box-shadow: 0 0 0 1px var(--input-soft-border-color-focus) inset;   /* = 0 0 0 1px alpha(light-dark(#0d0d0d, #ffffff), 20%) inset */
      }

      &[data-invalid] {
        box-shadow: 0 0 0 1px var(--input-border-color-invalid) inset;   /* = 0 0 0 1px light-dark(#e02e2a, #ba2623) inset */
      }
    }
  }

  /* =============================================
    Invalid
  ============================================= */
  &[data-invalid] {
    ::selection {
      background-color: var(--red-a50);   /* = alpha(#fa423e, 16%) */
    }
  }

  /* =============================================
    Disabled
  ============================================= */
  &[data-disabled] {
    cursor: not-allowed;
    opacity: 0.5;
  }
}

.Textarea {
  display: block;
  width: 100%;
  max-width: 100%;
  min-height: var(--textarea-min-height);   /* = calc(
    2px + (20px * var(--textarea-min-rows))
  ) */
  max-height: var(--textarea-max-height);   /* = calc(
    2px + (20px * var(--textarea-max-rows))
  ) */
  padding: var(--textarea-gutter-y) var(--textarea-gutter-x);   /* = 1px 8px */
  border: 0;
  border-radius: 0;
  appearance: none;
  background: none;
  box-shadow: none;
  color: inherit;
  /* Prevent Firefox autofill coloring (preview and confirmed modes) */
  filter: none;
  font-size: inherit;
  font-weight: inherit;
  line-height: var(--textarea-line-height);   /* = 20px */
  resize: none;

  &:focus {
    outline: 0;
  }

  &:disabled {
    cursor: not-allowed;
  }

  &::placeholder {
    color: var(--input-placeholder-text-color);   /* = light-dark(light-dark(#8f8f8f, #414141), light-dark(#414141, #8f8f8f)) */
    /* Normalize placeholder coloring across browsers. Firefox specifically uses `opacity` instead of a different `color`. */
    opacity: 1;
  }

  /* Applying an animation on autofill allows us to listen for the event in JS */
  &:-webkit-autofill {
    animation: native-autofill-in 1ms;
  }

  /* Prevent auto-fill coloring */
  &:-webkit-autofill,
  &:-webkit-autofill:focus,
  &:-webkit-autofill:hover {
    color: var(--color-text) !important;   /* = light-dark(#0d0d0d, #ffffff) !important */
    -webkit-text-fill-color: var(--color-text) !important;   /* = light-dark(#0d0d0d, #ffffff) !important */
    /* Prevent all color changes by delaying their effect for a very long time */
    /* We cannot beat the specificity of user-agent stylesheet declarations, so we must delay them. */
    transition:
      background-color 0s 50000s,
      box-shadow 0s 50000s,
      filter 0s 50000s !important;
  }
  /* Identical to block above, but must keep separate for browsers to recognize their own selector */
  &:-webkit-autofill-and-obscured,
  &:-webkit-autofill-strong-password,
  &:-webkit-autofill-strong-password-viewable {
    color: var(--color-text) !important;   /* = light-dark(#0d0d0d, #ffffff) !important */
    -webkit-text-fill-color: var(--color-text) !important;   /* = light-dark(#0d0d0d, #ffffff) !important */
    transition:
      background-color 0s 50000s,
      box-shadow 0s 50000s,
      filter 0s 50000s !important;
  }
  /* Prevent strange autofill size adjustments */
  &:-webkit-autofill::first-line {
    font-size: 16px !important;
    transition: font-size 0s 50000s !important;
  }

  &::-webkit-resizer {
    visibility: hidden;
  }
}

@keyframes native-autofill-in {
  0% {
    opacity: 1;
  }

  100% {
    opacity: 1;
  }
}
